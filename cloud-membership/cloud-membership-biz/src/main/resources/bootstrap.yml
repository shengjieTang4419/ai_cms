spring:
  application:
    name: cloud-membership
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yml
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
